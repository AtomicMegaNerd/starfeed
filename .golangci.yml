---
version: "2"
linters:
  default: standard
  enable:
    - lll
    - gocognit
  settings:
    lll:
      line-length: 100
      tab-width: 4
    gocognit:
      min-complexity: 15
  exclusions:
    rules:
      - path: _test\.go$
        linters:
          - gocognit
